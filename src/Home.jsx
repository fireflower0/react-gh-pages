import React from 'react';
import styled from 'styled-components';

const FormWrapper = styled.div``;

const Home = () => {
  return (
    <FormWrapper>
      Home
    </FormWrapper>
  );
};

export default Home;
